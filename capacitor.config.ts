import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hotel-restuarant-man',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
